CSV templates for import

Place rows under each template CSV in this `templates/` folder. If you want SQLite to auto-assign `id`, leave the `id` cell empty or omit the `id` column.

Headers match the current database column names exactly.
